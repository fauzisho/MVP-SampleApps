# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SimpleMVP' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SimpleMVP
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 4.7'
  
  target 'SimpleMVPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SimpleMVPUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
